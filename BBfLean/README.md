## BBfLean

Here is a summary of this directory:

* `FM.lean`: Contains definitions and utilities for proving FMs.
* `Size20Summary.lean`: Summary of all the proofs in `Size20`.
* `Size21Summary.lean`: Summary of all the proofs in `Size21`.
* `Size22Summary.lean`: Summary of all the proofs in `Size22`.
* `Size20`: Contains proofs of FMs of size 20.
* `Size21`: Contains proofs of FMs of size 21.
* `Size22`: Contains proofs of FMs of size 22.
* `CLAUDE.md`: A tutorial on how to write Lean proofs. This is entirely written by Claude Opus 4.6. Sorry in advance if this tutorial contains slop.

### Size 20

In `Size20`, there is a proof that all 6 FMs in `sz20_6.txt` are nonhalting:
```
[4/15, 3/14, 275/2, 7/11, 3/5]
[5/6, 4/35, 539/2, 3/11, 5/7]
[5/6, 539/2, 4/35, 3/11, 5/7]
[5/6, 44/35, 49/2, 3/11, 5/7]
[28/15, 3/22, 25/2, 11/7, 3/5]
[5/6, 49/2, 44/35, 3/11, 5/7]
```

The files are numbered from `Sz20_6_1.lean` to `Sz20_6_6.lean`. These files were generated by <https://github.com/int-y1/BBFractran/blob/main/other/gen_lean.py> and the proof of `sorry` was filled in manually.

### Size 21

The files numbered from `Sz21_140_unofficial_1.lean` to `Sz21_140_unofficial_140.lean` were generated by <https://github.com/int-y1/BBFractran/blob/main/other/gen_lean.py>.

All 140 FMs have been proven to be non-halting.

Among all Opus 4.6 proofs, the top 2 hardest FMs by token count are: #19 (181k tokens), #15 (167k tokens). (Unfortunately, token count is influenced by LLM luck.)

### Size 22

The files numbered from `Sz22_2003_unofficial_1.lean` to `Sz22_2003_unofficial_1600.lean` were generated by <https://github.com/int-y1/BBFractran/blob/main/other/gen_lean.py>.

1594/1600 were proven to be non-halting. The rest are:
* #29, #41, #430: Fenrir (probviously non-halting Cryptid).
* #1070, #1094: Halts in 1463418236 steps. (Thanks @Peacemaker II for the step count) (TODO: get a 2nd confirmation)
* #1554: Halts in ??? steps. (TODO: get the exact step count. The step count might be between 10^11 and 10^12)

---

There are 2003 FMs in `sz22_2003_unofficial.txt`, so this will take a lot of Opus 4.6 tokens.

Prompt that I used for size 22:
```
Make sure the Lean environment is set up correctly, and that `~/proofs/BBfLean/Size21Summary.lean` compiles. Afterwards, you should repeat this process until every `sorry` in `~/proofs/BBfLean/Size22/` is filled in:
* Pick the lowest-numbered file in `~/proofs/BBfLean/Size22/` that contains a `sorry`.
* Launch an agent on this file to fill in the `sorry`. Give these additional instructions when launching the agent: First, read `~/proofs/BBfLean/CLAUDE.md` for guidance on proofs. Use `/tmp/` to store temporary files. Do not clone any directories. Write your proof to the file in `~/proofs/BBfLean/Size22/`. After you finish writing the proof, make sure that there are no warnings, no lint messages, no info messages, no uses of `set_option`, and no rambling comments.
* Repeat until 2 agents are launched. If an agent finishes, you should launch a new agent to maintain 2 agents.
* When an agent finishes, you should verify the proof using `lake`. If the proof fails verification, DON'T fix the proof, and instead, launch a 2nd agent to rewrite the proof. If the proof still fails verification, give up on this file, and DON'T launch a 3rd agent.
```

I used "Repeat until 2 agents" to make sure Claude Opus 4.6 doesn't reach the rate limit. However, Claude Opus 4.6 sometimes reaches the rate limit anyways.

The average holdout took ~85k tokens. The hardest 1% of holdouts took >270k tokens. (Unfortunately, the hardest holdouts had bad LLM luck and used much more tokens than necessary.)
